/**
 * The Kestrel engine version, surfaced in run reports and the CLI banner.
 * Kept in sync with package.json by release tooling.
 */
export const VERSION = '0.1.0';

/** Human-readable engine identifier used in logs and digests. */
export const ENGINE = 'kestrel';
